# Action Scheduler CLI

> Due to two years of delays on the PRs submitted to Action Scheduler, creating this plugin seemed the next best thing to optimizing my use (and hopefully yours) of Action Scheduler.

## Commands

- `action`
  - `new`
  - `delete`
  - `list`
- `version`
- `status`